<Page
    x:Class="Planel.Views.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Planel.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image  VerticalAlignment="Bottom" HorizontalAlignment="Left" Source="/Assets/Headings/h4.png" Stretch="UniformToFill"      />
        <StackPanel  Margin="0,40,0,0" >
            <Grid>
                <Ellipse   HorizontalAlignment="Center" Height="100"  Width="100" >
                    <Ellipse.Fill>
                        <ImageBrush Stretch="UniformToFill" x:Name="avatar"  />
                    </Ellipse.Fill>
                </Ellipse>
                <Button Click="Button_Click_1" BorderBrush="Gray" BorderThickness="1" Background="Transparent" Foreground="White" 
                  FontFamily="Segoe MDL2 Assets" FontSize="30" Content="" Style="{StaticResource dotbut}" Height="100" Width="100" HorizontalAlignment="Center" />

            </Grid>
            <TextBlock Margin="0,70,0,0" Foreground="White" TextAlignment="Center" FontSize="20" Text="Let's be familiar" />
            <StackPanel>
                <TextBox x:Name="namebox" MaxWidth="400" TextAlignment="Justify" PlaceholderText="YOUR NAME"  Margin="5,10,5,0" Height="40" VerticalAlignment="Top" TextChanged="namebox_TextChanged" />
            </StackPanel>
        </StackPanel>
        <HyperlinkButton x:Name="logout" Content="LOG OUT" HorizontalAlignment="Center"  VerticalAlignment="Bottom" FontSize="20" Width="180" Foreground="{StaticResource SystemControlBackgroundAccentBrush}" Click="logout_Click" Margin="159,0,155,10"/>
    </Grid>
</Page>
