<UserControl
    x:Class="Denna.Controls.IconItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Denna.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="600">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Grid.Column="0" Foreground="{x:Bind Brush,Mode=OneWay}" HorizontalAlignment="Center" Text="{x:Bind Glyph,Mode=OneWay}" FontSize="{x:Bind FontSizeGlyph,Mode=OneWay}" FontFamily="Segoe MDL2 Assets" />
        <TextBlock VerticalAlignment="Center" Grid.Column="1" HorizontalAlignment="Left" Text="{x:Bind Text,Mode=OneWay}" FontSize="15" Opacity="0.7" />


    </Grid>
</UserControl>
