<Page
    x:Class="Denna.Views.SubMaster.TimeLine"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Denna.Views.SubMaster"
    xmlns:Controls="using:Denna.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:VM="using:Denna.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <VM:TimeLineViewModel />
    </Page.DataContext>
    <Grid Background="{ThemeResource AppBackgroundColor}">
        <ScrollViewer>
            <StackPanel>
                <StackPanel  >
                    <TextBlock FontSize="14" FontWeight="SemiBold" Opacity="0.8" Margin="20,0,0,0" Text="Today" />
                    <Controls:TaskList TaskLists="{x:Bind VM.TodayList,Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        
    </Grid>
</Page>
